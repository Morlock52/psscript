# Frontend production example
# Copy to src/frontend/.env.local for local overrides or configure the same values in Netlify/Render/Railway.

VITE_API_URL=https://api.example.com/api
VITE_DISABLE_AUTH=false
VITE_DEMO_EMAIL=
VITE_DEMO_PASSWORD=
