Update driveupload files with new additions and renames

Removed several old files from .tmp.driveupload, added new files, and performed multiple file renames with updated content. This refreshes the .tmp.driveupload directory with the latest set of files and reorganizes existing ones.
