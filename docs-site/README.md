# PSScript docs site

This folder contains the Docusaurus site that renders the documentation in `/docs-site/docs`.

## Structure

- `docs/` contains curated documentation organized by Diátaxis.
- `static/images/` contains screenshots and graphics.
- `src/css/custom.css` defines the documentation theme.

## Quick start

```bash
cd docs-site
npm install
npm run start
```
